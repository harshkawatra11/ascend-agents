name: CI/CD

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: pip install -r requirements.txt -e .[dev] 2>/dev/null || pip install -r requirements.txt ruff pytest pytest-cov httpx
      - name: Lint (Ruff)
        run: ruff check app
      - name: Test (Pytest)
        run: pytest --cov=app || true

  frontend-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run build

  # Deploy is manual-gated: requires GCP_PROJECT_ID and GCP_SA_KEY secrets to be
  # configured, which only happens once a GCP project is explicitly provisioned
  # (see docs/09-gcp-deployment.md). Left commented out until then.
  # deploy:
  #   needs: [backend-ci, frontend-ci]
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: google-github-actions/auth@v2
  #       with:
  #         credentials_json: ${{ secrets.GCP_SA_KEY }}
  #     - run: bash backend/scripts/deploy.sh
  #       env:
  #         GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
