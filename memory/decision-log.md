# Decision Log

| Date | Decision | Why |
|---|---|---|
| 2026-07-05 | Project named **SwasthyaGrid AI**, authors Harsh Kawatra & Dayita Arora | Sounds like infrastructure, not another chatbot |
| 2026-07-05 | Repo: public, `harshkawatra11/SwasthyaGrid-gdg-buildwithai`, pushed via `gh` CLI | Judges need frictionless read access |
| 2026-07-05 | Documentation-first workflow: `docs/` written before any code | Repository is single source of truth; enables handoff without conversation history |
| 2026-07-05 | Monorepo: `frontend/` (Next.js) + `backend/` (FastAPI) | Keeps one repo for judges to browse while giving each layer a clean-architecture home |
| 2026-07-05 | Gemini used only for explanation, never prediction | Keeps forecasts auditable/deterministic; avoids hallucinated numbers |
| 2026-07-05 | Core AI principle: AI proposes, human approves/rejects/modifies — enforced at API layer (recommendations always start `pending`) | This is the hackathon's stated winning angle; must be structurally true, not just a UI label |
| 2026-07-05 | Design system: beige/warm-neutral editorial palette, serif+sans pairing, risk colors reserved for signal only | Explicit user mandate: no blue-purple SaaS look, must feel agency-grade |
| 2026-07-05 | Mock-data-first: deterministic mock forecast/recommendation generators instead of trained ML models | This is a prototyping round; judged on concept/UX of the human-AI loop, not model accuracy. Real XGBoost/LightGBM deferred, designed in [[docs/06-forecasting]] |
| 2026-07-05 | GCP deployment scaffolded but not executed this session | No GCP project authenticated yet; Gemini AI Studio key available now. User directive: only spend GCP credits / Gemini 2.5 Flash free tier — stop and flag anything else needing payment |
| 2026-07-05 | CI/CD workflow file (`.github/workflows/ci-cd.yml`) left uncommitted at end of session | `gh` CLI token lacked the `workflow` OAuth scope; user began `gh auth refresh -s workflow` device-code flow but didn't complete it before session end. File exists locally, untracked. See [[next-tasks]] for the exact recovery command |
| 2026-07-05 | Transformed the frontend from a single-scroll page into a routed multi-page dashboard (persistent sidebar + topbar shell, `(dashboard)` route group, 10 pages) | User explicitly said the one-pager "is a project and not a hackathon scoped prototype" and wanted a dashboard-style control room with a premium, agency-grade UI. Locked via AskUserQuestion: routed pages (not tabs), role switcher (no real auth), backend-wired with mock fallback, full premium UX (drawer/⌘K/toasts/transitions) |
| 2026-07-05 | Recommendation state lifted from component-local `useState` into a shared `RecommendationsProvider` context | Session 1 left `RecommendationsPanel` and `ResourceTransfers` on separate local state so approvals didn't propagate; this was flagged in `next-tasks.md` and fixed as part of the dashboard rework since multiple routed pages now need the same live state |
| 2026-07-05 | Facility risk-level "live downgrade" heuristic: target facility downgrades on approval, except `bed_redirect` where the *source* facility is the overloaded one | Bed-redirect recommendations name the overloaded facility as `source` (it offloads to `target`'s capacity), the reverse of every other recommendation type — verified by re-testing in-browser after the first (target-only) version incorrectly left the overloaded facility red |
| 2026-07-05 | Removed all mentor-repo references from the public submission | User's explicit instruction — a clean, independent submission with no citation of the workshop reference repo |
| 2026-07-05 | Ask feature on Vercel calls a new Next.js `/api/ask` route directly with Gemini, instead of the FastAPI backend | No FastAPI backend is deployed to Vercel (frontend-only deploy); a same-origin serverless route makes "Ask" actually work in production. Grounded via the mock district JSON inlined into the prompt rather than tool-calling, since a lightweight serverless function doesn't need the full agent loop |
| 2026-07-05 | Declined to extract the Vercel CLI's stored auth token to call the API directly for setting Project Root Directory | Correctly flagged as credential-hunting by the harness's auto-mode classifier; used the documented root-level `vercel.json` (`buildCommand: cd frontend && npm run build`) workaround instead, which achieves the same result through a legitimate, committed config file |
| 2026-07-05 | Deployed frontend to Vercel as project `swasthyagrid`, aliased to `swasthyagrid.vercel.app`, git-connected for auto-deploy | User's explicit domain request; `GEMINI_API_KEY` intentionally left unset in Vercel env pending the user providing it |
